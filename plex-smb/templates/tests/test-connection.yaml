apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plex-smb.fullname" . }}-test-connection"
  labels:
    {{- include "plex-smb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plex-smb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
