apiVersion: v1
kind: Service
metadata:
  name: {{ include "plex-smb.fullname" . }}
  labels:
    {{- include "plex-smb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plex-smb.selectorLabels" . | nindent 4 }}
