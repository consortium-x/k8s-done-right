apiVersion: v1
kind: Service
metadata:
  name: plex-smb
  namespace: plex # This Must be configured for your cluster setup
spec:
  type: LoadBalancer
  selector:
    app: plex
  ports:
  - name: smb
    port: 445
    targetPort: 445
    protocol: TCP